# SevenTools::Fraction::getValue

```cpp
template <class U = double>
constexpr U getValue() const noexcept;
```

返回 `(U)getNumerator() / getDenominator()`

## 返回值

`(U)getNumerator() / getDenominator()`